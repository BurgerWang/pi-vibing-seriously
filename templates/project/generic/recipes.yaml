# Fallback asset only. /q-init generic replaces this with a stack-detected
# recipe snapshot. When no unambiguous executable convention can be inferred it
# deliberately leaves recipes empty instead of emitting misleading npm tasks.
# NOT_CONFIGURED: declare project-specific argv-array recipes explicitly.
recipes: []
